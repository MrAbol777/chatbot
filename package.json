{
  "name": "hemraz-chatbot",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "node scripts/dev-with-logs.cjs",
    "install-all": "npm install --prefix frontend && npm install --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "axios": "^1.17.0"
  }
}
