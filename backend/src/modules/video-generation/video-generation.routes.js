const express=require('express'); const jwt=require('jsonwebtoken'); const rateLimit=require('express-rate-limit'); const { createAuthMiddleware }=require('../image-generation/auth.middleware'); const { createVideoGenerationRepository }=require('./video-generation.repository'); const { createVideoQuotaService }=require('./quota/video-quota.service'); const { createVideoGenerationService }=require('./video-generation.service'); const { createVideoGenerationController }=require('./video-generation.controller'); const { createVideoContentController }=require('./video-content.controller'); const { createVideoContentAuthController }=require('./video-content-auth.controller'); const { createMetisVideoProvider }=require('./providers/metis-video.provider'); const { createFakeVideoProvider }=require('./providers/fake-video.provider'); const { loadVideoStorageConfig }=require('./storage/video-storage.config'); const { createLocalVideoStorage }=require('./storage/local-video.storage');
function createVideoGenerationRouter(deps){ const router=express.Router(); const env=deps.env||process.env; const storageConfig=loadVideoStorageConfig(env); const useFake=env.NODE_ENV==='test'&&env.VIDEO_FAKE_PROVIDER==='1'; const provider=useFake?createFakeVideoProvider():createMetisVideoProvider({httpClient:deps.httpClient,baseUrl:env.METIS_BASE_URL||env.METIS_VIDEO_BASE_URL,apiKey:env.METIS_API_KEY||env.METIS_VIDEO_API_KEY,requestTimeoutMs:Number(env.METIS_REQUEST_TIMEOUT_MS||120000),statusTimeoutMs:Number(env.METIS_STATUS_TIMEOUT_MS||30000),resultAllowedHosts:storageConfig.allowedHosts,resultAllowedPorts:storageConfig.allowedPorts,resultAllowedPathPrefixes:storageConfig.allowedPathPrefixes,resultTimeoutMs:storageConfig.timeoutMs,resultMaxBytes:storageConfig.maxBytes,resultMaxRedirects:storageConfig.maxRedirects}); const service=createVideoGenerationService({repository:createVideoGenerationRepository(deps.db),quotaService:createVideoQuotaService({db:deps.db,plansRepository:deps.plansRepository}),provider,isFeatureEnabled:()=>String(env.VIDEO_GENERATION_ENABLED||'0')==='1'}); const c=createVideoGenerationController(service); const storage=createLocalVideoStorage(storageConfig); const content=createVideoContentController({service,storage,jwtSecret:deps.authJwtSecret}); const contentAuth=createVideoContentAuthController({service,jwtSecret:deps.authJwtSecret}); const submitLimiter=rateLimit({windowMs:60_000,max:10,standardHeaders:true,legacyHeaders:false,keyGenerator:(req)=>String(req.user?.id||req.ip)}); router.use(createAuthMiddleware({jwtSecret:deps.authJwtSecret,db:deps.db})); router.use((req,_res,next)=>{ const token=req.cookies?.[deps.adminCookieName||'admin_token']; if(token&&deps.adminJwtSecret){ try { req.videoAdmin=jwt.verify(token,deps.adminJwtSecret); } catch(_){} } next(); }); router.get('/options',c.options); router.post('/',submitLimiter,c.submit); router.get('/',c.list); router.get('/:generationId/content-auth',contentAuth); router.get('/:generationId/content',content); router.head('/:generationId/content',content); router.get('/:generationId',c.get); return {router,service,storage}; }
module.exports={createVideoGenerationRouter};
